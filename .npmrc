
strict-peer-dependencies=false
shell-emulator=true
shamefully-hoist=true
# 忽略项目中引入的各个modules之间的相同modules,但不同版本的问题并继续安装
# legacy-peer-deps=true
# 指定项目依赖的来源（yarn 会读取.npmrc的配置文件，所以不必为yarn再设置一次）
# registry= https://registry.npm.taobao.org/
